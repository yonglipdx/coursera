In .vimrc

   #change git default editor:
   git config --global core.editor [to_gvim_path_without_exe]

   #change gvim background color: 
   colorscheme koehler

   # Make vim compatible with Chinese character
   set encodding=utF8
   set guifontwide=NSimSun


